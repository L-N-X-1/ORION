# Local dev overrides — hot reload + exposed ports
version: "3.9"
services:
  digital-twin:
    volumes:
      - ./services/digital-twin:/app
  collector:
    volumes:
      - ./services/collector:/app
  actuator:
    volumes:
      - ./services/actuator:/app
  api-gateway:
    volumes:
      - ./services/api-gateway:/app
  ai-agent:
    volumes:
      - ./services/ai-agent:/app
